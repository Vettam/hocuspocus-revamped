# Server Configuration
EXPRESS_PORT=3000
DEBUG=true

# This will be used by docker and traefik
# to configure DNS and SSL
PUBLIC_HOST=collaboration.api.vettam.app

# JWT Configuration (REQUIRED)
# SECURITY: JWT_SECRET must be at least 32 characters long
JWT_SECRET=your-very-long-jwt-secret-key-minimum-32-characters
JWT_ALGORITHM=HS256
JWT_ISSUER=https://<project-id>.supabase.co/auth/v1

# Vettam API Configuration (REQUIRED)
VETTAM_API_URL=https://api.vettam.app
VETTAM_API_KEY=your-api-key-here
VETTAM_API_TIMEOUT=30000

# CORS Configuration
# Comma-separated list of allowed origins
# Supports wildcard patterns: https://*.vettam.app, http://*.example.com
# Examples:
#   Single origin: CORS_ORIGIN=https://app.vettam.app
#   Multiple origins: CORS_ORIGIN=https://app.vettam.app,https://admin.vettam.app
#   Wildcard pattern: CORS_ORIGIN=https://*.vettam.app
#   Mixed: CORS_ORIGIN=https://*.vettam.app,https://external.com
#   All origins (dev only): CORS_ORIGIN=*
CORS_ORIGIN=*
CORS_CREDENTIALS=true
